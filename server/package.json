{
  "name": "health-log-server",
  "version": "1.0.0",
  "description": "Daily health log — diabetic / post-transplant care",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "generate-keys": "node ../scripts/generate-keys.js",
    "add-user": "node ../scripts/add-user.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.21.0",
    "node-cron": "^3.0.3",
    "web-push": "^3.6.7"
  },
  "engines": { "node": ">=20" }
}
