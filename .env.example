# Copy to .env and fill in. Generate the VAPID_* values with:
#   node scripts/generate-keys.js
VAPID_PUBLIC=
VAPID_PRIVATE=
VAPID_SUBJECT=mailto:you@example.com

# Base URL magic links are built against — must be the real HTTPS domain in production
PUBLIC_URL=https://health.yourdomain.com
